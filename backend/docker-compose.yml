services:
  swagger-ui:
    image: swaggerapi/swagger-ui:v5.17.14
    container_name: burs-swagger
    ports:
      - "8081:8080"
    environment:
      SWAGGER_JSON: /openapi.yaml
      BASE_URL: /
    volumes:
      - ./openapi.yaml:/openapi.yaml
    restart: unless-stopped
